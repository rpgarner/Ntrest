import {Link} from "react-router-dom";

const Navbar = (props) => {

    return (
        <header>
            <nav>
                <p>test</p>
            </nav>
        </header>
    )
}

export default Navbar